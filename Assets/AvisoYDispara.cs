using UnityEngine;
using System.Collections;

public class AvisoYDispara : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 2 );
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
